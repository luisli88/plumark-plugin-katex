{
  "name": "markdown-editor-plugin-katex",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=browser --loader:.css=text --outfile=dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "katex": "^0.16.11"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.7.3"
  }
}
